import MuiButtion from '@mui/material/Button'
import styled from '@emotion/styled';

export const SkillButton = styled(MuiButtion)((props) => ({
  backgroundColor: 'darkgrey',
  color: 'lightgrey',
  margin: '2px'
}));